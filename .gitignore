# Build output. Everything here is regenerated by `make build`; the objects,
# dependency files, kernel image and any captured screenshots all live under it.
build/

# Local working notes, kept out of the published repo. The design invariants it
# recorded now live in README.md.
NOTES.md

# Editor and OS noise
*.swp
*~
.DS_Store
